# Telegram Bot Core

Core de um Bot de Telegram em Node.js para Vercel Edge Functions com Firebase.

## Setup

1. `npm install`
2. Configure as variáveis de ambiente no `.env` (local) ou Vercel.
3. Deploy para Vercel.

## Comandos
- `/start`: Captura o lead e envia DM de boas-vindas.
- `/broadcast <mensagem>`: Envia mensagem para todos os leads (Admin Only).
